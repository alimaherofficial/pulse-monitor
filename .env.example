# ============================================
# Pulse Monitor - Environment Configuration
# ============================================

# Application
# --------------------------------------------
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# Database (PostgreSQL)
# --------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://pulse:pulse_password@localhost:5432/pulse_monitor?schema=public"

# PostgreSQL Docker Compose settings (for local dev)
POSTGRES_USER=pulse
POSTGRES_PASSWORD=pulse_password
POSTGRES_DB=pulse_monitor

# Redis (for BullMQ queues)
# --------------------------------------------
REDIS_URL=redis://localhost:6379

# Authentication (NextAuth.js / GitHub OAuth)
# --------------------------------------------
# Get from GitHub Developer Settings > OAuth Apps
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000

# Telegram Bot (for alerts)
# --------------------------------------------
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Optional: Postmark/Resend for email alerts
# --------------------------------------------
# EMAIL_PROVIDER=postmark
# POSTMARK_API_KEY=your_postmark_api_key
# EMAIL_FROM=noreply@yourdomain.com

# Optional: Discord webhook defaults
# --------------------------------------------
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
