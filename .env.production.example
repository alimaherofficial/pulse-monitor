# ============================================
# Pulse Monitor - Production Environment Configuration
# ============================================

# Application
# --------------------------------------------
NODE_ENV=production
PORT=3000
APP_URL=https://your-domain.com

# Database (PostgreSQL)
# --------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For Railway/Render, they provide this automatically
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# PostgreSQL Docker Compose settings (for local prod testing)
POSTGRES_USER=pulse
POSTGRES_PASSWORD=change_this_to_a_secure_password
POSTGRES_DB=pulse_monitor

# Redis (for BullMQ queues)
# --------------------------------------------
# For Railway/Render, use their Redis service or Upstash
REDIS_URL=redis://localhost:6379

# Authentication (Passport + GitHub OAuth)
# --------------------------------------------
# Get from GitHub Developer Settings > OAuth Apps
# Callback URL should be: https://your-api-domain.com/auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# JWT Secrets - Generate strong random strings
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_min_32_chars
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_min_32_chars

# Telegram Bot (for alerts) - Optional
# --------------------------------------------
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Frontend URL (for CORS and redirects)
# --------------------------------------------
FRONTEND_URL=https://your-frontend-domain.com

# Optional: Postmark/Resend for email alerts
# --------------------------------------------
# EMAIL_PROVIDER=postmark
# POSTMARK_API_KEY=your_postmark_api_key
# EMAIL_FROM=noreply@yourdomain.com

# Optional: Discord webhook defaults
# --------------------------------------------
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
